{
  "name": "create-playwright-pro",
  "version": "1.0.0",
  "bin": {
    "create-playwright-pro": "./dist/cli.js" 
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}